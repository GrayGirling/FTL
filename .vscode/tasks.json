{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            //"command": "/usr/bin/gcc",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            //"options": {
            //    "cwd": "/usr/bin"
            //}
        }
    ],
    "version": "2.0.0"
}